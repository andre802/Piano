A browser synthesizer using Tone.js, a Web audio frameowrk, and tonal, a music theory library along with HTML, CSS, and JavaScript. Bundled with webpack, packages installed with npm. Bundled files placed in dist folder, including single html file.

Build with 'npm run build' in command line, which will generate the bundle.js files.